Checkpoint 12 work record
=========================

Scope
-----

Checkpoint 12 implements bounded operational validation rather than the final
production schedule. Recovery drills and global systemd deployment belong to
checkpoint 13.

Implemented material
--------------------

- `storage.s3_spool_cleanup` selects only canonical derived-image keys older
  than an exact cutoff. It supports dry-run and bounded deletion, excludes
  unknown and backup keys, paginates listings, batches deletes, and emits
  persistent cleanup metrics.
- `database.database_backup` produces a full PostgreSQL custom-format dump,
  uploads it below `backups/postgresql/`, verifies size and SHA-256 metadata,
  and emits persistent backup metrics. It never restores a dump.
- Pushgateway metrics and the Operational batch jobs dashboard cover cleanup
  and backup outcome, duration, stages, objects, and byte volume.
- postgres_exporter, node_exporter, and cAdvisor are pinned in Compose and
  scraped by Prometheus. Infrastructure and validation-worker alert rules and
  the Infrastructure health dashboard are provisioned.
- Validation-only systemd units run three five-job-bounded continuous workers
  and a non-overlapping ten-minute maintenance cycle. Discovery is dry-run in
  Windy, Fintraffic, Skaping order, with Windy restricted to Denmark. It is
  followed by real one-hour spool cleanup and a real database backup.

Safety boundaries
-----------------

- Cleanup derives age only from a fully validated immutable image key.
- Database backups cannot match the cleanup key grammar.
- The first live cleanup must begin with a dry-run; the exact bounded deletion
  scope must be shown to the owner before deletion.
- Validation units are not production scheduling policy.
- Database restoration and destructive volume operations are absent.

Completed live validation
-------------------------

- A bounded 24-hour cleanup deleted 10 canonical Windy image objects dated
  2026-07-21, totaling 212,528 bytes. There were no deletion failures and the
  next dry-run returned different keys.
- A bounded one-hour cleanup deleted 10 canonical Fintraffic image objects
  dated 2026-07-23, totaling 441,404 bytes. There were no deletion failures
  and the next dry-run returned different keys.
- Two full database dumps were successfully stored and verified:

      backups/postgresql/2026/07/24/webcam_ingestion_20260724T083830Z.dump
      backups/postgresql/2026/07/24/webcam_ingestion_20260724T084605Z.dump

  Their respective sizes were 4,038,271 and 3,927,716 bytes.
- Prometheus reported `up=1` for postgres_exporter, node_exporter, cAdvisor,
  and the MQTT exporter. Cleanup and backup metrics were present.
- 158 relevant repository tests passed and 22 provider/live tests were
  skipped. Eleven legacy FastAPI upload tests remain independently stale:
  their fixture patches the removed `api.main.IngestToPipeline` attribute.

Owner validation
----------------

The validation-only systemd units were installed and exercised by the owner.
The first startup exposed systemd's restricted default PATH: `just` was found
through its absolute path, but recipes exited with status 127 because `uv` was
not discoverable. Validation application units now explicitly run as
`codexuser` and set PATH to include `/home/codexuser/.local/bin`.
Administrative privileges remain limited to unit installation and control.

All three bounded ingestion services then ran continuously, exposed metrics on
ports 8113, 8114, and 8115, and respected the five-job epoch limit. The Windy
process command confirmed the additional Denmark-only restriction.

The first corrected maintenance cycle completed successfully. Its cleanup
removed an exceptional pre-existing backlog of 1,499,349 canonical images
(29,884,445,384 bytes) in 2,652.75 seconds with no failures, after which the
database backup completed successfully:

    backups/postgresql/2026/07/24/webcam_ingestion_20260724T133250Z.dump

The following maintenance cycle also completed successfully and without
overlap. Once the backlog was absent, cleanup examined 290 objects and deleted
four eligible images (67,408 bytes) in 0.20 seconds with no failures. This
confirmed the expected steady-state cleanup behaviour.

The owner accepted checkpoint 12 on 2026-07-24. The accelerated timer was
disabled and the three bounded ingestion workers were stopped. Production
orchestration, graceful worker-stop policy, restoration, and recovery drills
remain checkpoint 13 work.
